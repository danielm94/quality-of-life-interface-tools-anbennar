custom_button = {
	name = parliament_macro_button
	potential = {
		always = yes
	}
	trigger = {
		FROM = {
			NOT = {
				has_country_flag = parliament_macro_event_open
			}
			NOT = {
				has_country_flag = cant_manually_add_parliament_seats
			}
		}
	}
	effect = {
		FROM = {
			set_country_flag = parliament_macro_event_open
			country_event = {
				id = parliament_macro.0
			}
		}
	}
}
